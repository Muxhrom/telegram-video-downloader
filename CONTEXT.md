# Telegram 视频下载器

本项目帮助单个用户浏览 Telegram 视频，并管理其本地和云端副本。以下术语用于区分视频的来源、下载历史和当前文件位置。

## 术语

**视频条目**：某个 Telegram 聊天中的一条含视频的消息。聊天与消息共同确定条目；文件名不能单独确定它的身份。

**已浏览视频**：用户曾在程序中加载过的视频条目。再次打开聊天时可先显示已有列表，再检查是否出现新条目。

**曾下载**：视频条目有一次完成的下载记录。此状态不表示本地文件现在仍然存在。
_避免_：用“已下载”同时表示下载历史和本地文件存在。

**本地副本**：当前仍在本地存储的视频文件。它可以在下载后被用户删除，也可以因压缩而替换原片。

**云端副本**：当前仍在用户云盘中的视频文件。它可以在本地副本被删除后继续存在。

**确认云端匹配**：云端副本与某个视频条目的对应关系已有可靠记录并通过核验。此状态不依赖本地副本是否存在。

**疑似云端匹配**：程序发现同一聊天下有名称相符的云端文件，但还没有足够证据确认它对应哪个视频条目。
_避免_：将同名文件直接称为“已备份”。

**云端未核实**：当前无法确认某个视频条目是否仍有云端副本，例如云盘暂时不可访问。此状态不等于云端文件不存在。
